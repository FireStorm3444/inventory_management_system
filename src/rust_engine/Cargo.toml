[package]
name = "rust_engine"
version = "0.1.0"
edition = "2024"

[lib]
name = "rust_engine"
# Crucial: Tells rustc to output a Python-compatible shared object (.so)
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.29.0", features = ["extension-module", "abi3-py312"] }
